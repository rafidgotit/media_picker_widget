name: media_picker_widget
description: A widget that picks media files from storage and allows you to place anywhere in the widget tree.
version: 1.0.8-pre.1
homepage: https://github.com/rafidgotit/media_picker_widget

environment:
  sdk: ">=2.12.0 <=3.10.1"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  photo_manager: ^3.0.0-dev.2
  sliding_up_panel: ^2.0.0+1
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: